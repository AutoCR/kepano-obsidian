---
created: 2026-06-22
tags:
  - note
  - journal
---
- 烟台
	- [x] 触发多端路径接车时，方向lock和normal来回切换，导致无法触发ssep搜索，车辆停止不动。取消多端路径倒车接车。
- 前港
	- [x] WBCA增加路径长度，增加route的间距（5m改为10m），增加采样间隔（0.5m改为1m），减少耗时。同时发现增加route间距，可以显著提升求解的成功率，减少路径跳动
	- [x] 优化warm start。warm start激活后，同样的输入，求解几次之后必会失败，而且求解时间也变长了。尝试从历史和参考线的加权和热启动，发现没啥用。后面把route间距改成10m，解决了。
	- [x] 增加连个route point最大行驶时间和最小行驶时间代价，以代替最大速度和最小速度代价。从惩罚速度改为惩罚时间，对minco更友好，梯度计算更简单。但实测无显著提升，后续遇到异常case再，咱不启用。
- 高速pnc架构
- 小马预控的验证