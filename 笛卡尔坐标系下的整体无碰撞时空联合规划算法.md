---
created: 2026-06-15
tags:
  - 0🌲
---

# 提案基本信息

**提案名称：** 笛卡尔坐标系下的整体无碰撞时空联合规划算法（Whole-Body Collision-Avoidance Spatio-Temporal Joint Planning in Cartesian Coordinates，简称 WBCA）

**技术领域：** 自动驾驶 / 运动规划（Planning & Control）；适用于港口、码头等结构化但狭窄、低速、多障碍场景下 L4 级商用车辆（普通乘用车 / 卡车、拖挂集卡、前后轮转向 AGV / IMV）的局部轨迹规划。

---

## 背景技术

### 应用场景及要解决的技术问题

港口/码头自动驾驶作业中，车辆需在狭窄通道、堆场、闸口、装卸点等强约束空间内完成会车、绕障、靠泊、调头等机动。与开放道路相比，该场景有三个突出难点：

1. **车体大、余量小。** 拖挂集卡总长可达 16~18 m，AGV 宽度大，作业通道两侧常仅有数十厘米余量。若仅以车辆中心点或少量角点做碰撞检测，会因车体扫掠区域（尤其转弯时）估计不足而发生剐蹭。
2. **时间与空间强耦合。** 低速精细机动时，"走哪条线"与"何时走到、走多快"必须联合决定：限速由路径曲率决定，而到达时间又反过来约束可行的横向偏移与曲率。先规划路径再单独规划速度（路径-速度解耦）会丢失这种耦合，导致结果次优甚至不可行。
3. **车型差异大。** 普通前轮阿克曼车、拖挂集卡（铰接体）、前后轮转向 AGV 三者运动学差异显著（单刚体 vs. 双刚体铰接 vs. 双转向冗余），需要一套可复用、又能精确表达各自约束的统一框架。

### 现有技术及缺点

- **基于 Frenet 坐标系（s-l）的横纵分层规划：** 工业界主流（如 Apollo EM Planner）。优点是把曲线道路"拉直"便于采样；但缺点在港口场景尤为致命：(a) 依赖结构化参考线，堆场内常无清晰车道线；(b) s-l 到笛卡尔的非线性映射在大横移、大曲率、倒车时严重畸变，车体几何（尤其拖挂车体）难以精确表达；(c) 横纵解耦丢失时空耦合。
- **基于栅格的搜索（Hybrid A\*）：** 能在笛卡尔系直接搜索且考虑运动学，但输出为离散、不光滑路径，曲率/速度不连续，且仅做几何路径、不含时间维度，需后续平滑与速度规划，难以保证整体最优与动力学可行。
- **传统优化类规划：** 多采用质点或包络盒近似车体，碰撞约束往往线性化或仅在采样点的中心检测，"整车体"安全性不足；多数将时间固定或单独分配，未做时空联合优化；且通常只针对单一车型，缺乏对铰接体、双转向体的统一支持。

---

## 本提案的基本思路

本提案提出一种**完全在笛卡尔坐标系下、对整车体（whole-body）进行无碰撞约束、并对空间路径与时间分配进行联合优化**的运动规划算法。相对现有技术，主要改进有四点：

1. **笛卡尔系直接规划，摆脱 Frenet 依赖。** 全流程（前端搜索、安全走廊、后端优化）均在笛卡尔 (x, y) 下进行，不需要参考线做 s-l 映射，天然适配无车道线的堆场以及大横移/倒车机动。

2. **整车体（Whole-Body）碰撞规避。** 用沿车体纵轴稠密布置的一组覆盖圆（dense vertices）来表示整车占据，每个圆心随车辆朝向旋转后逐一对欧氏符号距离场（ESDF）做带梯度的距离查询，从而约束**整个车体表面**而非中心点或四角，转弯扫掠区域也被严格覆盖。对拖挂集卡，牵引车与挂车两个刚体各自布置覆盖圆，挂车位姿由铰接角动力学解析推得。

3. **时空联合优化。** 采用 MINCO（Minimum-Control）稀疏轨迹参数化，将**空间控制点 {ξᵢ} 与各段时间分配 {Tᵢ} 放入同一决策向量**，在单一可微目标函数中同时优化几何形状与时间，并通过 ESDF 安全走廊/距离场把碰撞、曲率、速度、加速度、横向加速度等全部表达为光滑罚函数，用拟牛顿（L-BFGS）/信赖域求解。

4. **车型可插拔的统一框架。** 前端搜索、走廊生成、MINCO 后端为公共基类；三种车型仅通过重写"运动学/车体覆盖/特有约束"少量接口接入：普通车为单刚体二维 (x,y) 轨迹；拖挂集卡增加挂车铰接角 β 的一阶动力学与挂车体碰撞项；AGV 把后轮转向角 β 升维为第三维轨迹分量 (x,y,β)，并施加前/后轮双转向角约束与"蟹行（crab）"协调约束。

---

## 本提案的详细描述

### 1. 总体流程

本提案是一种方法。整体为"前端几何搜索 → 路径+速度锚点提取 → 安全走廊生成 → MINCO 时空联合优化 → 输出带时间戳的光滑轨迹"的流水线，如图 1 所示。

```tikz
\begin{tikzpicture}[
  node distance=7mm,
  box/.style={draw, rounded corners, align=center, minimum width=46mm, minimum height=10mm, font=\small, fill=blue!5},
  io/.style={draw, align=center, minimum width=46mm, minimum height=9mm, font=\small, fill=gray!10},
  arr/.style={-{Latex[length=2.2mm]}, thick}
]
\node[io] (map) {占据栅格 / ESDF 符号距离场\\(障碍、边界)};
\node[box, below=of map] (front) {前端：Hybrid A\* 三维搜索\\(x, y, \theta)，运动学基元，整车体碰检};
\node[box, below=of front] (route) {路径\&速度锚点提取\\(笛卡尔锚点 + 速度-时间约束 $\{v_i,T_i\}$)};
\node[box, below=of route] (sc) {安全走廊生成 (FIRI / Galaxy)\\半空间多面体 $A x \le b$};
\node[box, below=of sc] (opt) {后端：MINCO 时空联合优化\\空间控制点 $\xi$ 与时间 $T$ 同时优化};
\node[io, below=of opt] (out) {输出：光滑、动力学可行、\\带时间戳的整车无碰轨迹};
\draw[arr] (map)--(front);
\draw[arr] (front)--(route);
\draw[arr] (route)--(sc);
\draw[arr] (sc)--(opt);
\draw[arr] (opt)--(out);
\node[right=8mm of front, font=\scriptsize, align=left, draw, dashed, rounded corners, fill=yellow!8] (k1)
  {\textbf{创新点1}\\笛卡尔系直接搜索\\整车体碰撞检测};
\node[right=8mm of opt, font=\scriptsize, align=left, draw, dashed, rounded corners, fill=yellow!8] (k2)
  {\textbf{创新点2/3}\\时空联合 + 整车体软罚\\车型可插拔};
\draw[arr, dashed] (k1)--(front);
\draw[arr, dashed] (k2)--(opt);
\end{tikzpicture}
```
**图 1　WBCA 算法整体流程**

下面按步骤描述。

---

### 2. 步骤一：前端 Hybrid A\* 几何搜索（笛卡尔系，整车体碰撞）

在连续状态空间 $(x, y, \theta)$ 中进行混合 A\* 搜索，得到一条满足车辆运动学、整车无碰的几何引导路径。

**2.1 状态空间与离散化**
- 位置 $(x,y)$ 连续，按 1.0 m × 1.0 m 栅格哈希；朝向 $\theta\in[0,2\pi)$ 离散为 72 个方向（5° 一格）。

**2.2 运动学基元（节点扩展）**　采用单车（自行车）模型前向积分。预定义直行与左/右两个转向基元 $(\Delta x,\Delta y,\Delta\theta)$，从节点 $(x,y,\theta)$ 扩展后继：

$$
\begin{aligned}
x' &= x + \Delta x\cos\theta - \Delta y\sin\theta,\\
y' &= y + \Delta x\sin\theta + \Delta y\cos\theta,\\
\theta' &= \mathrm{norm}(\theta + \Delta\theta).
\end{aligned}
$$

等价的转向半径 $r=\Delta x/\tan(\Delta\theta)$（工程取最小转弯半径约 6 m）。支持前进/倒车各三个基元，倒车与换向通过代价惩罚抑制。

**2.3 代价与启发**　移动代价
$$
g = g_{\text{prev}} + \ell\cdot p_{\text{turn}}^{[\text{转向}]}\cdot p_{\text{rev}}^{[\text{倒车}]} + p_{\text{cod}}^{[\text{换向}]},
$$
其中 $\ell$ 为基元弧长，$p_{\text{turn}}{=}1.05,\ p_{\text{rev}}{=}2.0,\ p_{\text{cod}}{=}2.0$（指数为对应布尔条件）。启发 $h$ 取与目标距离较近时的 Dubins 曲线长度与"含障碍 2D A\*"启发的较大者，保证可采纳性。

**2.4 整车体碰撞检测（创新点 1）**　不同于仅检测中心点，本步对车辆位姿 $(x,y,\theta)$ 处的整车占据进行检测：将车体沿纵轴稠密离散为一组覆盖点 $\{l_k\}$，逐点经旋转 $R(\theta)$ 平移到全局后查询 ESDF，要求 $D(R(\theta)l_k + (x,y)) \ge 0$。覆盖点数目与间距由车长/车宽确定（见 §5）。

---

### 3. 步骤二：路径与速度锚点提取（时空耦合）

从前端路径采样得到后端优化所需的笛卡尔锚点序列，并同步生成**速度-时间约束**，使空间间隔与时间增量在源头即耦合。

**3.1 速度剖面生成**　由参考限速（来自路径曲率）与加减速上限，用分段恒加速度模型生成纵向速度剖面。给定段距 $s$、首末速 $v_0,v_e$、加速度 $a$，行驶时间按运动学闭式给出（梯形/三角速度曲线）：
$$
t=\frac{\sqrt{v_0^2+2as}-v_0}{a}\quad(\text{加速段}),\qquad
v(s)=\sqrt{v_0^2+2as}.
$$

**3.2 锚点与时空约束**　沿前端路径推进时，**同时**用"按距离查时间 $v_t(s)$"与"按时间查距离 $v_s(t)$"两种方式取更保守者作为下一锚点，保证每段同时满足最小空间间隔与最大时间步。对每段记录一对约束 $\{v_i,\,T_i\}$（限速与该段名义时长），作为后端的速度约束与时间初值。这一步把"路径"与"速度"在采样层面就绑定在一起，为后端时空联合优化提供一致的初值与约束。

---

### 4. 步骤三：安全走廊生成（半空间多面体 $Ax\le b$）

为给后端提供凸的、可微的无碰可行域，沿锚点序列生成一串凸多面体安全走廊。每个多面体以 H-表示（半空间交）给出：
$$
\mathcal{P}_k=\{x\in\mathbb{R}^2\mid A_k x\le b_k\}.
$$

采用 **FIRI（Fast Iterative Region Inflation）** 在边界约束内对种子点做体积最大化膨胀，同时以障碍点云的切超平面把障碍排除在外；其内部用"最大体积内切椭球（MVIE）"子问题（L-BFGS + 光滑 L1 罚 + 对数行列式正则保证体积与正定性）迭代求解。在对速度要求更高的场合，可用基于凸包对偶（Galaxy）的快速构造替代。生成的 $A_k,b_k$ 作为后端的走廊约束（见 §6.3.3）。

---

### 5. 整车体（Whole-Body）几何建模

这是"整车无碰"的几何基础，三种车型共用同一思想：用车体局部坐标下的一组**稠密覆盖点**逼近车体外形。

车体外接矩形四角为
$$
\begin{aligned}
&l_1=(+L_f,+W_l),\quad l_2=(-L_r,+W_l),\\
&l_3=(-L_r,-W_r),\quad l_4=(+L_f,-W_r),
\end{aligned}
$$
其中 $L_f,L_r$ 为基准点到车头/车尾距离，$W_l,W_r$ 为左右半宽。沿纵轴均匀布置 $N=\lceil L_f/W\rceil+\lceil L_r/W\rceil+1$ 个覆盖点（间距约一个车宽 $W$，每点等效覆盖半径 $W/2$）：
$$
l_k^x=\begin{cases}k\cdot \Delta_f, & 1\le k\le N_f\\[2pt] -(k-N_f)\cdot \Delta_r,& N_f<k\le N\end{cases},\qquad l_k^y=0.
$$

```tikz
\begin{tikzpicture}[scale=0.9]
% vehicle body rotated
\begin{scope}[rotate=20]
  \draw[thick, fill=blue!6] (-2.2,-0.9) rectangle (3.2,0.9);
  % dense covering circles along longitudinal axis
  \foreach \x in {-1.8,-1.0,-0.2,0.6,1.4,2.2,2.9}{
    \draw[red!70, dashed] (\x,0) circle (0.9);
    \fill[red] (\x,0) circle (1.2pt);
  }
  \draw[-{Latex}, thick] (0,0)--(3.6,0) node[right]{车体纵轴 $x_{\text{body}}$};
  \node at (3.0,1.25) {$\theta$};
\end{scope}
% world frame
\draw[-{Latex}] (-3,-2.4)--(-1.8,-2.4) node[right]{$x$};
\draw[-{Latex}] (-3,-2.4)--(-3,-1.2) node[above]{$y$};
% caption-ish labels
\node[align=left, font=\scriptsize] at (1.5,-2.6)
  {每个红色圆 = 一个覆盖点 $l_k$，半径 $W/2$；\\随朝向 $\theta$ 旋转后逐一对 ESDF 查询距离与梯度};
\end{tikzpicture}
```
**图 2　整车体稠密覆盖点模型（普通车 / AGV）**

某覆盖点的全局位置与对应 ESDF 距离约束：
$$
l_k^{\text{world}} = (x,y)^\top + R(\theta)\,l_k,\qquad D(l_k^{\text{world}}) \ge d_{\min},
$$
其中朝向 $\theta=\operatorname{atan2}(\dot y,\dot x)$ 由轨迹一阶导给出，旋转矩阵 $R(\theta)=\begin{bmatrix}\cos\theta&-\sin\theta\\\sin\theta&\cos\theta\end{bmatrix}$。

---

### 6. 步骤四：后端 MINCO 时空联合优化（核心）

#### 6.1 决策变量与轨迹参数化

决策向量同时含**空间内点**与**时间**：
$$
\mathbf{x}=[\,\xi_1,\dots,\xi_{M-1},\ \tau_1,\dots,\tau_{M}\,]^\top,
$$
$\xi_i$ 为第 $i$ 个内部路点坐标（普通车/卡车为 $(x,y)$，AGV 为 $(x,y,\beta)$），$\tau_i$ 为第 $i$ 段时间的无约束参数。为保证时间为正，采用双向光滑映射 $T=\mathcal{C}(\tau)$：
$$
T_i=\begin{cases}(0.5\tau_i+1)\tau_i+1, & \tau_i>0,\\[4pt]\dfrac{1}{(0.5\tau_i-1)\tau_i+1}, & \tau_i\le 0,\end{cases}
\qquad
\frac{\mathrm dT_i}{\mathrm d\tau_i}=\begin{cases}\tau_i+1, & \tau_i>0,\\[6pt]\dfrac{1-\tau_i}{\big((0.5\tau_i-1)\tau_i+1\big)^2}, & \tau_i\le0.\end{cases}
$$

轨迹用 **MINCO**（每段五次多项式）表示。第 $i$ 段 $x_i(t)=\sum_{j=0}^{5}c_{ij}t^j,\ t\in[0,T_i]$。给定路点序列与时间 $(\,\{\xi\},\{T\}\,)$，多项式系数 $\mathbf{c}$ 由一个带状线性系统唯一确定：
$$
M(T)\,\mathbf{c}=\mathbf{b}(\xi),
$$
其中 $M(T)$ 编码两端边界条件（位置/速度/加速度）、相邻段 $C^2$ 连续与路点穿越约束，带宽固定，可 $O(M)$ LU 分解。这种"由路点+时间隐式确定系数"的稀疏参数化，使变量数从系数级降到路点级，是高效时空联合优化的关键。

#### 6.2 目标函数总览

$$
J=\underbrace{w_E E}_{\text{能量/光滑}}+\underbrace{w_T\!\sum_i T_i}_{\text{时间}}+\underbrace{J_\kappa}_{\text{曲率/转角}}+\underbrace{J_{\text{col}}}_{\text{整车碰撞}}+\underbrace{J_{\text{sc}}}_{\text{安全走廊}}+\underbrace{J_{\text{dev}}}_{\text{路径贴合}}+\underbrace{J_v+J_a+J_{a_\perp}}_{\text{运动学限}}+\underbrace{J_{\text{type}}}_{\text{车型特有}}.
$$

所有不等式约束统一转化为**光滑 L1 罚（SmoothedL1）**，使问题无硬约束、处处可微：
$$
\mathcal{L}_\mu(g)=\begin{cases}0,& g<0,\\[2pt](\mu-\tfrac{g}{2})\big(\tfrac{g}{\mu}\big)^3,& 0\le g\le\mu,\\[4pt] g-\tfrac{\mu}{2},& g>\mu,\end{cases}
\qquad
\mathcal{L}'_\mu(g)=\begin{cases}0,&g<0,\\[2pt]\big(\tfrac{g}{\mu}\big)^2\big(3\tfrac{\mu-g/2}{\mu}-\tfrac12\tfrac{g}{\mu}\big),&0\le g\le\mu,\\[4pt]1,&g>\mu.\end{cases}
$$
其中 $g>0$ 表示违反约束的量（如 $g=d_{\min}-D$ 表示距离不足）。对沿段的积分罚用梯形求积近似：$\int_0^{T_i}f\,\mathrm dt\approx\sum_j \omega_j\,\delta t\,f(t_j)$，端点 $\omega_j{=}0.5$、内部 $\omega_j{=}1$，$\delta t{=}T_i/(K{-}1)$。

#### 6.3 各代价项与梯度推导

记每段第 $j$ 采样点的轨迹一/二/三阶导为 $\dot\sigma,\ddot\sigma,\dddot\sigma$，$B=\begin{bmatrix}0&-1\\1&0\end{bmatrix}$ 为 90° 旋转。各项对系数的梯度均通过"多项式基 $\times$ 物理量梯度"叠加到 $\partial J/\partial\mathbf c$，再由 MINCO 反传（§6.4）得到对 $\xi,T$ 的梯度。

**6.3.1 能量（最小控制）项**　$E=\sum_i\int_0^{T_i}\lVert\dddot\sigma_i(t)\rVert^2\mathrm dt$（最小 jerk）。其闭式与对系数、对时间的解析梯度为（仅列对 $c_3,c_4,c_5$ 的非零项）：
$$
E_i=36\lVert c_3\rVert^2 T_i+144\,c_3^\top c_4\,T_i^2+(192\lVert c_4\rVert^2+240\,c_3^\top c_5)T_i^3+720\,c_4^\top c_5\,T_i^4+720\lVert c_5\rVert^2 T_i^5,
$$
$$
\frac{\partial E_i}{\partial c_3}=72 c_3 T_i+144 c_4 T_i^2+240 c_5 T_i^3,\quad
\frac{\partial E_i}{\partial T_i}=36\lVert c_3\rVert^2+288 c_3^\top c_4 T_i+\dots+3600\lVert c_5\rVert^2 T_i^4.
$$
低速起步时对能量项乘以 $w_{\text{start-up}}$ 加权，抑制起步抖动。

**6.3.2 整车体碰撞项（创新点 2）**　对每段每采样点、对每个覆盖点 $l_k$，
$$
l_o = \sigma + R\,l_k,\quad g_c=d_{\min}-D(l_o),\quad
J_{\text{col}}=\sum w_c\,\omega_j\,\delta t\,\mathcal{L}_\mu(g_c),
$$
$R$ 由单位化速度方向构造（$R=[\,\hat{\dot\sigma}\ \ B\hat{\dot\sigma}\,]$）。其梯度链：对位置 $\partial g_c/\partial\sigma=-\nabla D$，对速度方向（即朝向）$\partial g_c/\partial\dot\sigma$ 经 $\partial l_o/\partial\theta$ 给出，工程实现为
$$
\nabla_{\text{pos}}=-\mathcal{L}'_\mu\,\nabla D,\qquad
\nabla_{\text{vel}}=B\,R\,l_k\;(\nabla_{\text{pos}})^\top\,\frac{B\dot\sigma}{\lVert\dot\sigma\rVert^2},
$$
再分别乘多项式基 $\Phi_0,\Phi_1$ 叠加到系数梯度；对时间 $T_i$ 的梯度含 $\delta t$ 直接项 $\mathcal{L}_\mu/(K{-}1)$ 与经 $t=\alpha T_i$ 的间接项。处于负距离（已侵入）时罚权 $w_c$ 额外乘 $w_c^{-}$ 加重。对动态障碍，则把多个覆盖点-障碍对的间隙用 **soft-min（log-sum-exp，$\alpha{<}0$）** 聚合后再罚，得到对"最危险点"光滑可微的近似：
$$
\mathrm{LSE}_\alpha(\mathbf{c})=d_0+\tfrac1\alpha\log\sum_k e^{\alpha(c_k-d_0)},\quad w_k=\frac{e^{\alpha(c_k-d_0)}}{\sum_m e^{\alpha(c_m-d_0)}},\quad \nabla=\sum_k w_k\nabla c_k.
$$

**6.3.3 安全走廊项**　对覆盖点 $l_o$ 与所属多面体每条边 $a_n^\top l_o-b_n$，用 soft-max（$\alpha{>}0$）聚合"是否越界"后罚 $\mathcal{L}_\mu$，梯度同理由 softmax 权重加权 $a_n$ 反传。该项与 §6.3.2 互补：走廊给凸可行域、ESDF 距离场给精细避障。

**6.3.4 曲率/转角项**　曲率
$$
\kappa=\frac{\ddot\sigma^\top B\,\dot\sigma}{\lVert\dot\sigma\rVert^3},\qquad
\frac{\partial\kappa}{\partial\dot\sigma}=\frac{B^\top\ddot\sigma}{\lVert\dot\sigma\rVert^3}-3\frac{(\ddot\sigma^\top B\dot\sigma)\,\dot\sigma}{\lVert\dot\sigma\rVert^5},\quad
\frac{\partial\kappa}{\partial\ddot\sigma}=\frac{B\dot\sigma}{\lVert\dot\sigma\rVert^3}.
$$
普通车以 $g=\kappa^2-\kappa_{\max}^2$ 入罚（$\kappa_{\max}=\tan\delta_{\max}/L$）。还可加曲率变化率 $\dot\kappa$ 罚以平滑转向。

**6.3.5 速度 / 加速度 / 横向加速度项**
$$
J_v=\!\int\!\big[\mathcal{L}_\mu(\lVert\dot\sigma\rVert^2-v_{\max}^2)+\mathcal{L}_\mu(v_{\min}^2-\lVert\dot\sigma\rVert^2)\big],\quad
J_a=\!\int\!\mathcal{L}_\mu\!\Big(\tfrac{(\dot\sigma^\top\ddot\sigma)^2}{\lVert\dot\sigma\rVert^2}-a_{\max}^2\Big),
$$
$$
J_{a_\perp}=\int\mathcal{L}_\mu\big(\lVert a_\perp\rVert^2-a_{\perp,\max}^2\big),\quad a_\perp=\ddot\sigma-(\ddot\sigma^\top\hat{\dot\sigma})\hat{\dot\sigma},
$$
各项梯度按上述 $\partial(\cdot)/\partial\dot\sigma,\partial(\cdot)/\partial\ddot\sigma$ 链式给出。

**6.3.6 路径贴合项**　$J_{\text{dev}}=\sum_i w_{\text{dev}}\,\lVert\xi_i-\xi_i^{\text{ref}}\rVert^2$，使结果不过度偏离前端引导路径（堆场可保持作业线）。

#### 6.4 梯度反传（MINCO 伴随）

各代价项给出 $\partial J/\partial\mathbf c$ 与 $\partial J/\partial T$（直接经 $\delta t$）后，通过对带状系统求伴随：解 $M(T)^\top\lambda=\partial J/\partial\mathbf c$，由 $\lambda$ 抽取得到 $\partial J/\partial\xi_i$，并叠加各段时间相关项得到 $\partial J/\partial T_i$；最后经 §6.1 的 $\mathrm dT/\mathrm d\tau$ 链式得到 $\partial J/\partial\tau$。整个反传复杂度随段数线性。

#### 6.5 求解器

采用 **L-BFGS**（历史 256，梯度/步长/函数值三重收敛判据）为默认求解器，强约束场合可切换**截断共轭梯度信赖域**。以 §3 的速度-时间约束做初值，迭代至代价低于阈值或满足收敛条件。

---

### 7. 三种车型的适配

三种车型共用前端搜索、走廊生成、MINCO 后端与上述公共代价项，仅在**运动学、车体覆盖、车型特有约束**三处差异化接入（图 3）。

```tikz
\begin{tikzpicture}[
  node distance=6mm and 10mm,
  base/.style={draw, rounded corners, fill=green!8, align=center, font=\small, minimum width=40mm, minimum height=12mm},
  leaf/.style={draw, rounded corners, fill=orange!8, align=center, font=\scriptsize, minimum width=38mm, minimum height=18mm},
  arr/.style={-{Latex[length=2mm]}}
]
\node[base] (b) {公共框架\\Hybrid A\* + 安全走廊 + MINCO 时空联合优化\\(能量/时间/速度/加速度/走廊/碰撞 软罚)};
\node[leaf, below left=12mm and -8mm of b] (car) {\textbf{普通前轮阿克曼车}\\单刚体二维 $(x,y)$\\曲率 $\kappa\!\le\!\tan\delta_{\max}/L$\\单一车体覆盖圆};
\node[leaf, below=12mm of b] (truck) {\textbf{拖挂集卡 (铰接)}\\牵引车 + 挂车两刚体\\铰接角 $\beta$ 一阶动力学\\挂车体单独覆盖+避障};
\node[leaf, below right=12mm and -8mm of b] (imv) {\textbf{前后轮转向 AGV}\\三维轨迹 $(x,y,\beta)$\\前/后轮双转角约束\\蟹行协调约束};
\draw[arr] (b)--(car);
\draw[arr] (b)--(truck);
\draw[arr] (b)--(imv);
\end{tikzpicture}
```
**图 3　车型可插拔的统一框架**

#### 7.1 普通前轮阿克曼转向车辆（单刚体）

最基础形态。状态为二维 $(x,y)$ 轨迹，朝向 $\theta=\operatorname{atan2}(\dot y,\dot x)$ 由一阶导隐式给出；车体用 §5 的单组覆盖圆表示；运动学约束即前轮转角约束 $\tan\delta=L\kappa$，以 §6.3.4 的曲率罚 $\kappa^2\le\kappa_{\max}^2$ 实现。其余代价项直接采用公共项。

#### 7.2 拖挂集卡（双刚体铰接）

```tikz
\begin{tikzpicture}[scale=0.95]
% tractor
\draw[thick, fill=blue!6] (0,0) rectangle (2.6,1.0);
\node at (1.3,0.5) {\scriptsize 牵引车};
% hitch point
\fill (0,0.5) circle (1.6pt);
\node[below left, font=\scriptsize] at (0,0.5) {铰接点};
% trailer rotated by beta about hitch
\begin{scope}[shift={(0,0.5)}, rotate=-22]
  \draw[thick, fill=orange!10] (-4.6,-0.5) rectangle (0,0.5);
  \node at (-2.3,0) {\scriptsize 挂车};
  \foreach \x in {-4.2,-3.4,-2.6,-1.8,-1.0,-0.3}{
     \draw[red!60,dashed] (\x,0) circle (0.5);
  }
  \draw[-{Latex}] (0,0)--(-0.9,0) node[above]{\scriptsize $x_{\text{tr}}$};
\end{scope}
% tractor heading
\draw[-{Latex}] (1.3,0.5)--(3.4,0.5) node[right]{\scriptsize $\theta$ (牵引车)};
% beta angle arc
\draw[->] (-0.9,0.5) arc (180:202:0.9);
\node[font=\scriptsize] at (-1.35,0.15) {$\beta$};
\node[align=left, font=\scriptsize] at (1.0,-1.2)
  {$\beta=\theta-\theta_{\text{tr}}$ 为铰接角；挂车体亦用稠密覆盖圆，\\位姿由铰接角动力学 (式) 解析推得后单独对 ESDF 避障};
\end{tikzpicture}
```
**图 4　拖挂集卡的铰接角与双刚体整车体建模**

- **挂车运动学（铰接角动力学）。** 设牵引车朝向 $\theta$、挂车朝向 $\theta_{\text{tr}}$，铰接角 $\beta=\theta-\theta_{\text{tr}}$。挂车朝向随时间演化（一阶 ODE，沿轨迹离散积分）：
$$
\dot\theta_{\text{tr}}=\frac{v\sin\beta+M\,\dot\theta\cos\beta}{L_{\text{tr}}},\qquad
\theta_{\text{tr}}(t{+}\delta t)=\theta_{\text{tr}}(t)+\dot\theta_{\text{tr}}\,\delta t,
$$
其中 $L_{\text{tr}}$ 为挂车轴距、$M$ 为铰接点到车头基点距离、$\dot\theta=\kappa v$ 为牵引车转向角速度。
- **挂车体整车碰撞。** 挂车覆盖点 $l_k$（挂车坐标）先经铰接旋转回到车头坐标再到全局：
$$
l_k^{\text{world}}=\sigma+R(\theta)\big(m+R(\beta)^\top l_k\big),\quad m=(M,0)^\top,
$$
然后对 ESDF 做与 §6.3.2 同构、并以 soft-min 聚合的碰撞罚，使**牵引车与挂车两刚体同时无碰**，并隐式抑制折叠（jackknife，$|\beta|$ 过大时挂车体快速逼近障碍而被罚）。
- **特有处理。** 平滑（低速精细）模式下可放开 $\beta$ 罚以允许更灵活的折叠机动。

#### 7.3 前后轮阿克曼转向 AGV / IMV（双转向冗余）

- **升维轨迹。** 后轮转向角 $\beta$ 作为**第三维轨迹分量**纳入 MINCO，控制点为 $(x,y,\beta)$，三维同样享受时空联合优化与 $C^2$ 连续。为数值稳定，对 $\beta$ 维用 $\beta=\operatorname{atan}(\xi_\beta)/c$ 的形变映射参数化（$c{=}4$），梯度按 $\partial\beta/\partial\xi_\beta=1/\big(c(1+\xi_\beta^2)\big)$ 反传。
- **前/后轮双转角约束。** 给定路径曲率 $\kappa$ 与后轮转角 $\beta$，前/后等效转角（$L_f{=}L_r{=}L/2$）：
$$
\tan\delta_f=\tan\beta+\frac{L_f\kappa}{\cos\beta},\qquad
\tan\delta_r=\tan\beta-\frac{L_r\kappa}{\cos\beta},
$$
分别以 $g_f=\tan^2\delta_f-\tan^2\delta_{\max}$、$g_r=\tan^2\delta_r-\tan^2\delta_{\max}$ 入 $\mathcal{L}_\mu$ 罚。其对 $\dot\sigma,\ddot\sigma$ 的梯度经 $\partial\kappa/\partial(\cdot)$（§6.3.4）链式，对 $\beta$ 维的梯度含 $\sec^2\beta\pm L\kappa\sin\beta\sec^2\beta$ 项。
- **蟹行（crab）协调约束。** 当前后轮同向偏转使车辆整体平移（朝向不随路径方向变化）时，在标记的蟹行段强制朝向贴近参考朝向：$g=\tan^2(\theta-\theta_{\text{ref}}-\beta)$ 入罚，实现"车身朝向保持、整体侧移"的港口对位机动。
- **车体覆盖。** 与普通车相同的单组覆盖圆，覆盖点经后轮转向角 $R(\beta)$ 旋转后再随车体朝向变换到全局做避障。

---

### 8. 算法效果与验证

- **整车安全性：** 因每条轨迹的每个采样点都对整车（含挂车）数十个覆盖点逐一查 ESDF 距离并入软罚，转弯扫掠区域被严格覆盖，在港口窄道实测可稳定保持厘米级安全余量而不剐蹭；中心点/四角法在同场景会低估车体占据。
- **时空最优与可行：** 空间形状与时间分配在同一目标内联合优化，输出轨迹位置/速度/加速度/曲率连续（MINCO $C^2$ 连续 + 软罚约束），直接可被下游控制跟踪，免去"先路径后速度"的二次拼接误差。
- **统一框架的工程价值：** 三种差异巨大的车型（单刚体、铰接双刚体、双转向）复用同一前端、走廊与后端，仅差异化运动学/车体/特有约束接口，显著降低维护成本并保证一致的安全与平顺指标。
- **可提供的验证数据：** 三种车型在港口仿真与实车回放下的（a）最小车体-障碍距离曲线，（b）曲率/前后轮转角是否越限，（c）速度/加速度/横向加速度包络，（d）单帧求解耗时统计，均可作为佐证一并提供。

---

### 9. 关键创新点小结（供撰写权利要求参考）

1. **笛卡尔系整车体（Whole-Body）无碰约束：** 以沿车体纵轴稠密布置、随朝向旋转的覆盖圆组逐点查询 ESDF 符号距离场并入光滑罚，约束整车表面而非中心/角点；前端搜索与后端优化均采用，无需 Frenet 映射。
2. **时空联合优化：** 以 MINCO 稀疏参数化把空间控制点与各段时间放入同一可微目标，结合正性时间映射与梯形求积，对几何与时间同时寻优；全部物理约束（碰撞、走廊、曲率、速度、加速度、横向加速度）以 SmoothedL1 + log-sum-exp 软化，无硬约束、处处可微，由 L-BFGS/信赖域求解。
3. **铰接体挂车的解析位姿 + 双刚体整车避障：** 由铰接角一阶动力学沿轨迹推得挂车位姿，对牵引车与挂车两组覆盖圆同时避障并隐式抑制折叠。
4. **双转向 AGV 的升维轨迹与双转角/蟹行约束：** 将后轮转角作为第三维纳入 MINCO 联合优化，施加前/后轮双转角上限与蟹行协调朝向约束。
5. **车型可插拔的统一规划框架：** 前端、走廊、后端公共化，三车型仅差异化少量运动学/车体/特有约束接口。
